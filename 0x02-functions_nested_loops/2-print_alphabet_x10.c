#include "main.h"
/**
 * print_alphabet_x10 - function to print chars
 *
 * returne = 0
 */
	void print_alphabet_x10(void)
{
	for (int i = 1 ; i <= 10 ; i++)
	{
	for (char b = 'a' ; b < = 'z' ; b++)
	{
	puchar(b);
	return (0);
	}
	return (0);
	}
